# This workflow isn't actually intended to be re-used - rather, it runs on this repo (how meta).

name: Update major release tag

on:
  release:
    types: [created]

jobs:
  tag-major-release:
    uses: TorqIT/github-actions-workflows/.github/workflows/update-major-release-tag.yml@v0